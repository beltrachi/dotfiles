#!/usr/bin/env ruby
# First arg is password length
LEN = (ARGV[0] || 16).to_i
txt = ""
range = ('a'..'z').to_a + ('A'..'Z').to_a + (((0..9).to_a))
LEN.times do
  txt += range.sample.to_s
end
puts txt
